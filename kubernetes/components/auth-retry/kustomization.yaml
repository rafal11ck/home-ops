---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component
configMapGenerator:
  - name: auth-retry-html
    files:
      - response.body=auth-retry.html
    options:
      disableNameSuffixHash: true
