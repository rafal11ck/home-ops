apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: keycloak
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      dashboards: grafana
  endpoints:
    - port: management
      path: /metrics
      scheme: http
